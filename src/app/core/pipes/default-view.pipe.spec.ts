import { DefaultViewPipe } from './default-view.pipe';

describe('DefaultViewPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultViewPipe();
    expect(pipe).toBeTruthy();
  });
});
